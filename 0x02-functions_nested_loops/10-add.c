#include "main.h"
/**
 * add - adds two integars and returns the result
 * @a: first integar
 * @b: second integar
 * Return: returns the sum of the two integars
 */
int add(int a, int b)
{
	return (a + b);
}
