#include "main.h"

/**
 * wildcmp - compares two string if they are identical
 * @s1: first string
 * @s2: second string
 * Return: returns 1 if the two strings are identical
 * otherwise returns 0.
 */
int wildcmp(char *s1, char *s2)
{
	if (s1 == s2)
		return (1);
	else
		return (0);
}
