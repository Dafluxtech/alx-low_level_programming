#include "main.h"
/**
 * _abs -  computes the absolute value of an integer.
 *@n: integar to make absolute
 * Return: absolute value of integar
 */
int _abs(int n)
{
	if (n < 0)
		n = -n;
	return (n);
}


